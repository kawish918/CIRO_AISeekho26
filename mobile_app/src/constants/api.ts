/**
 * CIRO API Configuration
 * 
 * UPDATE THIS WHEN YOUR LOCAL IP CHANGES:
 *   - Run `ipconfig` on Windows and look for "IPv4 Address"
 *   - Or check the Expo Metro URL (e.g. exp://192.168.100.51:8081 → IP is 192.168.100.51)
 * 
 * For production / APK distribution: replace with your hosted backend URL
 *   e.g. 'https://ciro-backend.onrender.com/api'
 */
export const API = 'http://192.168.100.51:8000/api';
